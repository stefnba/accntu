import { createFetch, createQueryFetch } from './fetch/create';
import { makeQueryFetch } from './fetch/make';
import { createMutation } from './mutation/create';

export { createFetch, createMutation, createQueryFetch, makeQueryFetch };
