import { AccountSection } from '../../_components/section';

const MultiFactorAuthSection = () => {
    return (
        <>
            <AccountSection
                title="Multi-Factor Authentication"
                action={<>Coming Soon...</>}
            />
        </>
    );
};

export default MultiFactorAuthSection;
