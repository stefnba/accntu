import {
    Sheet,
    SheetClose,
    SheetContent,
    SheetDescription,
    SheetFooter,
    SheetHeader,
    SheetTitle,
    SheetTrigger
} from '@/components/ui/sheet';

interface Props {}

export const ViewTransactionSheetContent: React.FC<Props> = ({}) => {
    return <SheetContent>asdffs</SheetContent>;
};
