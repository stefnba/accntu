export type TParsedTransaction = {
    // is_duplicate: boolean;
    // date: string;
    // title: string;
    // key: string;
    // account_amount: number;
    // account_currency: string;
    title: string;
    date: string;
    amount: number;
    key: string;
};
